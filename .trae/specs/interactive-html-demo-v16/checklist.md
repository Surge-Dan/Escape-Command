# Checklist

## HTML 骨架与视觉系统
- [x] `demo/index.html` 存在且为单文件（HTML+CSS+JS 内联）
- [x] 引入 Google Fonts `Noto Serif SC`（400/700 字重）
- [x] CSS 变量与小程序 v15 主题色统一（`#C8956E` 主色）
- [x] 手机框容器居中显示（375×812，圆角，阴影）
- [x] 底部 TabBar 3 个 Tab 可切换
- [x] 选中 Tab 高亮为暖棕褐色
- [x] 启动页 slogan「给城市一个随机出口」+ 进入按钮可用

## 首页
- [x] Hero 区暖色渐变背景（3 层径向渐变）
- [x] 顶部 pill 标签显示日期 + 地点
- [x] Hero 大标题为衬线宋体（700 字重）
- [x] 模式 Tag 可点击弹出 Sheet
- [x] Sheet 内 3 个模式选项可选
- [x] 出逃卡含骰子图标 + 脉冲环动画
- [x] 点击出逃卡触发摇骰子动画
- [x] 今日推荐显示 3 条精选指令

## 指令详情卡与执行页
- [x] 指令详情卡从底部滑出（translateY 动画）
- [x] 详情卡含类型标签 + 标题 + 元信息 + 小贴士
- [x] 收藏按钮可切换状态
- [x] 邀请朋友按钮在双人模式时显示
- [x] 邀请朋友与接受指令并排
- [x] 点击接受指令进入执行页
- [x] 执行页 4 步可逐步完成
- [x] 全部完成后显示「完成出逃」按钮
- [x] 点击完成后徽章解锁动画

## 地图页与我的页
- [x] 地图页有简化地图背景
- [x] 地图页有标记点
- [x] 地图页 Tab 可切换（地图/卫星/路线）
- [x] 我的页有用户卡片
- [x] 我的页有徽章墙
- [x] 我的页有统计数据

## 数据与交互
- [x] 精选 12 条指令覆盖 6 种类型
- [x] 摇骰子随机抽取指令
- [x] 模式过滤生效（微出逃=短时长 / 城市漫游=长时长）
- [x] 收藏状态可切换并同步到我的页

## 验证
- [x] 浏览器中打开 `demo/index.html` 所有功能正常
- [x] 摇骰子 → 指令卡 → 执行 → 完成全流程走通
- [x] Tab 切换正常
- [x] 桌面端手机框居中
- [x] 移动端全屏自适应
- [x] 配色为暖棕褐系
- [x] Hero 标题为衬线宋体

## 验证说明

- 静态审查：通读 `demo/index.html`（2120 行），核对 CSS 变量、字体引入、数据数组、动画 keyframes、HTML 结构
- 浏览器验证：使用 agent-browser 以 file:// 协议打开 Demo，完整走通启动页 → 首页 → 摇骰子 → 指令卡 → 执行页 → 4 步完成 → 徽章解锁流程；切换 Tab 验证地图页/我的页；切换视口验证桌面/移动端响应式
- Hero 渐变修复：原实现为 2 层径向 + 1 层线性，已修复为 3 层径向 + 1 层线性底色，严格满足 spec「3 层径向渐变叠加」要求
- 关于 `#5CBF9E`（绿）与 `#9B7BB8`（紫）：仅作为类型/模式区分色（继承自 v15 源码 `utils/constants.js` 的 TYPE_META 与 SHEET_MODES，spec 明确要求使用这两份数据），以及地图 pin 的装饰色；主品牌色、Hero 渐变、按钮、Tab 高亮等核心 UI 均为暖棕褐系（`#C8956E`）。邀请按钮使用 `.btn-secondary` 暖色填充（`#FAF3ED` 背景 + `#A87B52` 文字），符合 spec「邀请按钮改为填充暖色」要求
