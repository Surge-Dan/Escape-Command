/* ===== 导航栏（v11: 委托给 app.wxss 的全局 nav-header-*） ===== */
.group-room-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #FFF8F0 0%, #FFFFFF 100%);
}

/* ===== Loading ===== */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding-top: 200rpx;
}
.loading-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #5CBF9E;
  margin: 0 6rpx;
  display: inline-block;
  animation: dotBounce 1.4s infinite ease-in-out both;
}
.loading-dot:nth-child(1) { animation-delay: -0.32s; }
.loading-dot:nth-child(2) { animation-delay: -0.16s; }
.loading-text {
  margin-top: 20rpx;
  font-size: 26rpx;
  color: #999;
}
@keyframes dotBounce {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
  40% { transform: scale(1); opacity: 1; }
}

/* ===== Empty ===== */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 300rpx;
}
.empty-text {
  font-size: 30rpx;
  color: #999;
  margin-bottom: 40rpx;
}
.empty-back-btn {
  padding: 20rpx 60rpx;
  background: #5CBF9E;
  color: #fff;
  font-size: 28rpx;
  border-radius: 8rpx;
}

/* ===== Room Scroll ===== */
.room-scroll {
  height: calc(100vh - 88rpx);
}

/* ===== Room Hero ===== */
.room-hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx 32rpx 48rpx;
}
.room-id-label {
  font-size: 56rpx;
  font-weight: 800;
  letter-spacing: 8rpx;
  color: #2C2C2C;
}
.room-id-hint {
  font-size: 24rpx;
  color: #BBB;
  margin-top: 8rpx;
}
.room-topic {
  font-size: 36rpx;
  font-weight: 600;
  color: #2C2C2C;
  margin-top: 24rpx;
}
.room-meta {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 20rpx;
}
.room-status-tag {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
}
.status-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
}
.room-max-members {
  font-size: 24rpx;
  color: #999;
}
/* C-P3 联动：任务大厅出逃地点 */
.room-poi {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 20rpx;
  padding: 16rpx 24rpx;
  background: #F5F0E8;
  border-radius: 16rpx;
}
.room-poi-label {
  font-size: 22rpx;
  color: #999;
}
.room-poi-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #2C2C2C;
  margin-top: 6rpx;
}
.room-poi-address {
  font-size: 22rpx;
  color: #999;
  margin-top: 4rpx;
}

/* ===== Stage Section ===== */
.stage-section {
  padding: 0 32rpx 120rpx;
}

/* ===== Members Section ===== */
.members-section {
  background: #fff;
  border-radius: 16rpx;
  padding: 40rpx 32rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #2C2C2C;
  display: block;
  margin-bottom: 32rpx;
}
.section-subtitle {
  font-size: 24rpx;
  color: #999;
  display: block;
  margin-bottom: 20rpx;
}
.members-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 32rpx 0;
  justify-content: space-around;
}
.member-slot {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 25%;
}
.member-avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}
.member-avatar.empty-avatar {
  border: 2rpx dashed #DDD;
  background: transparent;
}
.member-icon {
  font-size: 36rpx;
  color: #fff;
}
.empty-avatar .member-icon {
  color: #CCC;
}
.member-name {
  font-size: 22rpx;
  color: #999;
  margin-top: 12rpx;
}
.waiting-hint {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  margin-top: 40rpx;
}
.waiting-dot {
  font-size: 40rpx;
  color: #5CBF9E;
  animation: dotPulse 1.5s infinite ease-in-out;
}
.waiting-dot:nth-child(2) { animation-delay: 0.2s; }
.waiting-dot:nth-child(3) { animation-delay: 0.4s; }
.waiting-text {
  font-size: 26rpx;
  color: #999;
  margin-left: 12rpx;
}
@keyframes dotPulse {
  0%, 100% { opacity: 0.3; }
  50% { opacity: 1; }
}

/* ===== Actions ===== */
.actions-section {
  margin-top: 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
}
.invite-btn {
  width: 100%;
  height: 96rpx;
  background: #5CBF9E;
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  border: none;
  line-height: 96rpx;
}
.invite-btn::after { border: none; }
.invite-icon {
  font-size: 32rpx;
}
.demo-actions {
  display: flex;
  gap: 20rpx;
  width: 100%;
}
.demo-btn {
  flex: 1;
  height: 76rpx;
  line-height: 76rpx;
  text-align: center;
  background: #FFF0E5;
  color: #D98A5C;
  font-size: 26rpx;
  border-radius: 8rpx;
}
.demo-btn.outline {
  background: transparent;
  border: 2rpx solid #D98A5C;
}
.start-vote-btn {
  width: 100%;
  height: 96rpx;
  background: #D98A5C;
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
}
.start-vote-btn .arrow {
  font-size: 36rpx;
}
/* C-09: 人齐按钮禁用态（<2 人）*/
.start-vote-btn.disabled {
  background: #E5C9B3;
  color: #fff;
}
/* C-09: READY 阶段到齐横幅 */
.ready-banner {
  background: #5C9EBF1A;
  border-radius: 16rpx;
  padding: 36rpx 32rpx;
  margin-bottom: 24rpx;
  text-align: center;
}
.ready-banner .ready-title {
  display: block;
  font-size: 34rpx;
  font-weight: 600;
  color: #5C9EBF;
  margin-bottom: 8rpx;
}
.ready-banner .ready-sub {
  display: block;
  font-size: 26rpx;
  color: #7A8B95;
}
.cancel-btn {
  font-size: 26rpx;
  color: #CCC;
  padding: 16rpx 32rpx;
}

/* ===== Preference Section ===== */
.preference-section {
  background: #fff;
  border-radius: 16rpx;
  padding: 40rpx 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.interest-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}
.interest-tag {
  padding: 16rpx 32rpx;
  background: #F5F5F5;
  border-radius: 8rpx;
  font-size: 26rpx;
  color: #666;
}
.interest-tag.selected {
  background: #5CBF9E1A;
  color: #5CBF9E;
  font-weight: 600;
}
.intensity-row {
  display: flex;
  gap: 16rpx;
}
.intensity-tag {
  flex: 1;
  padding: 20rpx 0;
  text-align: center;
  background: #F5F5F5;
  border-radius: 8rpx;
  font-size: 26rpx;
  color: #666;
}
.intensity-tag.selected {
  background: #D98A5C1A;
  color: #D98A5C;
  font-weight: 600;
}
.submit-pref-btn {
  margin-top: 32rpx;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  background: #7BAE7F;
  color: #fff;
  font-size: 28rpx;
  border-radius: 8rpx;
}

/* ===== Vote Cards ===== */
.vote-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 40rpx 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.vote-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #2C2C2C;
  display: block;
  margin-bottom: 28rpx;
}
.vote-options {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}
.vote-option {
  display: flex;
  align-items: center;
  padding: 24rpx 28rpx;
  background: #F8F8F8;
  border-radius: 12rpx;
  border: 2rpx solid transparent;
  gap: 12rpx;
}
.vote-option.selected {
  background: #FFF0E5;
  border-color: #D98A5C;
}
.vote-option-icon {
  font-size: 36rpx;
  margin-right: 8rpx;
}
.vote-option-label {
  font-size: 28rpx;
  color: #2C2C2C;
  font-weight: 500;
}
.vote-option-desc {
  font-size: 24rpx;
  color: #999;
  margin-left: 8rpx;
}
.vote-count {
  margin-left: auto;
  font-size: 24rpx;
  color: #5CBF9E;
  font-weight: 600;
}

/* ===== Vote Progress ===== */
.vote-progress {
  background: #fff;
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.progress-text {
  font-size: 26rpx;
  color: #666;
  display: block;
  margin-bottom: 16rpx;
}
.progress-bar {
  height: 12rpx;
  background: #F0F0F0;
  border-radius: 6rpx;
  overflow: hidden;
}
.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #5CBF9E, #7BAE7F);
  border-radius: 6rpx;
  transition: width 0.3s ease;
}

/* ===== Generate Button ===== */
.generate-btn {
  height: 96rpx;
  background: #D98A5C;
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
}
.generate-btn.disabled {
  background: #CCC;
}
.generate-btn .arrow {
  font-size: 36rpx;
}
.vote-hint {
  text-align: center;
  font-size: 24rpx;
  color: #BBB;
  margin-top: 16rpx;
}

/* ===== Script Stage ===== */
.script-stage {
  padding-top: 16rpx;
}
.script-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 48rpx 40rpx;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.06);
}
.script-badge {
  display: inline-block;
  padding: 6rpx 20rpx;
  background: #5CBF9E1A;
  color: #5CBF9E;
  font-size: 22rpx;
  font-weight: 600;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
}
.script-title {
  font-size: 40rpx;
  font-weight: 800;
  color: #2C2C2C;
  display: block;
  margin-bottom: 32rpx;
  line-height: 1.4;
}
.script-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 24rpx 0;
  padding: 24rpx 0;
  border-top: 2rpx solid #F5F5F5;
  border-bottom: 2rpx solid #F5F5F5;
  margin-bottom: 32rpx;
}
.meta-item {
  width: 50%;
  display: flex;
  flex-direction: column;
}
.meta-label {
  font-size: 22rpx;
  color: #BBB;
}
.meta-value {
  font-size: 28rpx;
  color: #2C2C2C;
  font-weight: 600;
  margin-top: 4rpx;
}
.script-steps {
  margin-bottom: 32rpx;
}
.steps-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #2C2C2C;
  display: block;
  margin-bottom: 24rpx;
}
.step-item {
  display: flex;
  align-items: flex-start;
  gap: 20rpx;
  margin-bottom: 24rpx;
}
.step-num {
  flex-shrink: 0;
  width: 44rpx;
  height: 44rpx;
  line-height: 44rpx;
  text-align: center;
  background: #D98A5C;
  color: #fff;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 50%;
}
.step-text {
  font-size: 28rpx;
  color: #2C2C2C;
  line-height: 1.6;
  padding-top: 4rpx;
}
.script-members {
  padding-top: 24rpx;
  border-top: 2rpx solid #F5F5F5;
}
.script-members-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}
.script-member-tag {
  padding: 8rpx 20rpx;
  background: #F5F5F5;
  border-radius: 8rpx;
  font-size: 24rpx;
  color: #666;
}
.script-cta {
  margin-top: 32rpx;
}
.start-escape-btn {
  height: 100rpx;
  background: linear-gradient(135deg, #5CBF9E, #7BAE7F);
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  box-shadow: 0 4rpx 16rpx rgba(92, 191, 158, 0.3);
}
.start-escape-btn .arrow {
  font-size: 36rpx;
}
/* C-12: 本次出逃记录入口 */
.view-record-cta {
  margin-top: 20rpx;
  height: 88rpx;
  background: #fff;
  border: 2rpx solid #5CBF9E;
  color: #2E8B6F;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
}
.view-record-cta .arrow {
  font-size: 34rpx;
}
.script-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 24rpx;
}
.regenerate-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  background: #fff;
  border: 2rpx solid #D98A5C;
  color: #D98A5C;
  font-size: 28rpx;
  border-radius: 8rpx;
}
.back-home-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  background: #5CBF9E;
  color: #fff;
  font-size: 28rpx;
  border-radius: 8rpx;
}

/* ===== v11: 邀请朋友弹窗 ===== */
.invite-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 48rpx;
  animation: inviteMaskIn 200ms ease;
}

@keyframes inviteMaskIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.invite-popup {
  position: relative;
  width: 100%;
  max-width: 600rpx;
  background: #fff;
  border-radius: 24rpx;
  padding: 48rpx 40rpx 40rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.18);
  animation: invitePopupIn 280ms cubic-bezier(0.34, 1.56, 0.64, 1.0);
}

@keyframes invitePopupIn {
  from { opacity: 0; transform: scale(0.85); }
  to { opacity: 1; transform: scale(1); }
}

.invite-popup-title {
  display: block;
  text-align: center;
  font-size: 34rpx;
  font-weight: 700;
  color: #2C2C2C;
  letter-spacing: 1rpx;
  margin-bottom: 16rpx;
}

.invite-popup-desc {
  display: block;
  text-align: center;
  font-size: 24rpx;
  color: #999;
  line-height: 1.5;
  margin-bottom: 32rpx;
}

.invite-room-card {
  background: #FFF8F0;
  border: 2rpx dashed #D98A5C;
  border-radius: 16rpx;
  padding: 28rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 32rpx;
}

.invite-room-label {
  font-size: 22rpx;
  color: #999;
  letter-spacing: 1rpx;
}

.invite-room-id {
  font-size: 56rpx;
  font-weight: 800;
  color: #2C2C2C;
  letter-spacing: 8rpx;
  font-family: "SF Mono", "Menlo", "Consolas", monospace;
}

.invite-actions {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.invite-action {
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 600;
  border-radius: 12rpx;
  letter-spacing: 1rpx;
  transition: all 180ms ease;
}

.invite-action.primary {
  background: #5CBF9E;
  color: #fff;
}

.invite-action.primary:active {
  background: #4DAE8D;
  transform: scale(0.98);
}

.invite-action.ghost {
  background: #FFF0E5;
  color: #D98A5C;
}

.invite-action.ghost:active {
  background: #FFE5D0;
  transform: scale(0.98);
}

.invite-hint {
  display: block;
  text-align: center;
  font-size: 22rpx;
  color: #BBB;
  line-height: 1.5;
  padding: 0 16rpx;
}

.invite-close {
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  color: #CCC;
  font-weight: 300;
}

.invite-close:active {
  color: #999;
}

/* ===== C-P4: Tab 区 ===== */
.cp4-tab-section {
  margin: 24rpx 32rpx 40rpx;
  background: var(--white);
  border-radius: 24rpx;
  box-shadow: 0 4rpx 24rpx rgba(0,0,0,0.04);
  overflow: hidden;
}
.cp4-tab-bar {
  display: flex;
  flex-direction: row;
  background: var(--brand-weak);
  padding: 8rpx;
  margin: 16rpx;
  border-radius: 100rpx;
}
.cp4-tab {
  flex: 1;
  text-align: center;
  padding: 16rpx 0;
  font-size: 26rpx;
  color: var(--ink-soft);
  border-radius: 100rpx;
  transition: all 0.2s;
}
.cp4-tab.active {
  background: var(--brand);
  color: var(--white);
  font-weight: 600;
}
.cp4-panel {
  padding: 8rpx 24rpx 24rpx;
  min-height: 400rpx;
}

/* 成员 Tab */
.cp4-member-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #F0EBE3;
}
.cp4-member-row:last-child {
  border-bottom: none;
}
.cp4-member-avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
}
.cp4-member-icon {
  color: var(--white);
  font-size: 28rpx;
}
.cp4-member-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.cp4-member-name {
  font-size: 28rpx;
  color: var(--ink);
  font-weight: 500;
}
.cp4-trust-tag {
  display: inline-block;
  align-self: flex-start;
  margin-top: 8rpx;
  padding: 4rpx 14rpx;
  border-radius: 10rpx;
  font-size: 22rpx;
  background: var(--brand-tint);
  color: var(--brand-dark);
}
.cp4-trust-gold {
  background: #FFF4D6;
  color: #B8860B;
}
.cp4-trust-reliable {
  background: #E8F5E9;
  color: #4E7A52;
}
.cp4-trust-newbie {
  background: var(--brand-tint);
  color: var(--brand-dark);
}
.cp4-trust-watch {
  background: #FBE9E7;
  color: #B85C3F;
}
.cp4-member-actions {
  display: flex;
  flex-direction: row;
  align-items: center;
}
.cp4-action-btn {
  font-size: 24rpx;
  color: var(--white);
  background: var(--brand);
  padding: 10rpx 20rpx;
  border-radius: 16rpx;
  margin-left: 12rpx;
}
.cp4-action-btn.ghost {
  background: transparent;
  color: var(--ink-faint);
  border: 1rpx solid #E0DBD3;
}
.cp4-action-btn:active {
  opacity: 0.7;
}
.cp4-review-hint {
  margin-top: 24rpx;
  padding: 20rpx;
  background: var(--brand-weak);
  border-radius: 16rpx;
  font-size: 24rpx;
  color: var(--brand-dark);
  text-align: center;
}

/* 聊天 Tab */
.cp4-chat-panel {
  display: flex;
  flex-direction: column;
  padding: 0;
  min-height: 500rpx;
}
.cp4-chat-scroll {
  height: 600rpx;
  padding: 16rpx 24rpx;
  box-sizing: border-box;
}
.cp4-chat-empty {
  text-align: center;
  padding: 80rpx 0;
  font-size: 24rpx;
  color: var(--ink-faint);
}
.cp4-msg-row {
  display: flex;
  flex-direction: column;
  margin-bottom: 20rpx;
}
.cp4-msg-row.mine {
  align-items: flex-end;
}
.cp4-msg-row.other {
  align-items: flex-start;
}
.cp4-msg-bubble {
  max-width: 70%;
  padding: 18rpx 24rpx;
  border-radius: 20rpx;
  display: flex;
  flex-direction: column;
}
.cp4-msg-row.mine .cp4-msg-bubble {
  background: var(--brand);
  color: var(--white);
  border-bottom-right-radius: 6rpx;
}
.cp4-msg-row.other .cp4-msg-bubble {
  background: var(--brand-weak);
  color: var(--ink);
  border-bottom-left-radius: 6rpx;
}
.cp4-msg-nickname {
  font-size: 22rpx;
  color: var(--ink-faint);
  margin-bottom: 6rpx;
}
.cp4-msg-content {
  font-size: 28rpx;
  line-height: 1.5;
}
.cp4-msg-pending {
  font-size: 20rpx;
  opacity: 0.7;
  margin-top: 4rpx;
  align-self: flex-end;
}
.cp4-chat-input-bar {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 16rpx 24rpx;
  border-top: 1rpx solid #F0EBE3;
}
.cp4-chat-input {
  flex: 1;
  height: 80rpx;
  background: var(--brand-weak);
  border-radius: 16rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  color: var(--ink);
}
.cp4-chat-send {
  margin-left: 16rpx;
  height: 80rpx;
  line-height: 80rpx;
  padding: 0 32rpx;
  background: var(--brand);
  color: var(--white);
  border-radius: 16rpx;
  font-size: 28rpx;
  font-weight: 500;
}
.cp4-chat-send.disabled {
  background: #D9C9B8;
}
.cp4-chat-send:active {
  opacity: 0.8;
}

/* 记录 Tab */
.cp4-record-panel {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 60rpx 24rpx;
}
.cp4-record-entry {
  width: 100%;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 32rpx;
  background: var(--brand-weak);
  border-radius: 20rpx;
  font-size: 28rpx;
  color: var(--brand-dark);
}
.cp4-record-entry .arrow {
  color: var(--brand);
}
.cp4-record-empty {
  font-size: 26rpx;
  color: var(--ink-faint);
  text-align: center;
}

/* ===== C-P4: 弹窗（评价/举报）===== */
.cp4-modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: flex-end;
  justify-content: center;
  z-index: 1000;
}
.cp4-modal {
  width: 100%;
  background: var(--white);
  border-radius: 24rpx 24rpx 0 0;
  padding: 40rpx 32rpx;
  padding-bottom: calc(40rpx + var(--safe-bottom));
  position: relative;
  animation: cp4SlideUp 0.25s ease;
}
@keyframes cp4SlideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}
.cp4-modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--ink);
  text-align: center;
}
.cp4-modal-target {
  font-size: 26rpx;
  color: var(--brand-dark);
  text-align: center;
  margin-top: 8rpx;
}
.cp4-stars {
  display: flex;
  flex-direction: row;
  justify-content: center;
  margin: 32rpx 0;
}
.cp4-star {
  font-size: 56rpx;
  color: #DDD;
  margin: 0 12rpx;
}
.cp4-star.on {
  color: #F5A623;
}
.cp4-tag-row {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  margin: 16rpx 0 24rpx;
}
.cp4-tag-chip {
  padding: 12rpx 24rpx;
  margin: 8rpx;
  background: var(--brand-weak);
  color: var(--ink-soft);
  border-radius: 10rpx;
  font-size: 24rpx;
}
.cp4-tag-chip.selected {
  background: var(--brand);
  color: var(--white);
}
.cp4-textarea {
  width: 100%;
  height: 160rpx;
  background: var(--brand-weak);
  border-radius: 16rpx;
  padding: 20rpx;
  font-size: 26rpx;
  color: var(--ink);
  box-sizing: border-box;
}
.cp4-modal-actions {
  display: flex;
  flex-direction: row;
  margin-top: 32rpx;
}
.cp4-modal-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  border-radius: 16rpx;
  font-size: 28rpx;
  margin: 0 8rpx;
}
.cp4-modal-btn.ghost {
  background: var(--brand-weak);
  color: var(--ink-soft);
}
.cp4-modal-btn.primary {
  background: var(--brand);
  color: var(--white);
  font-weight: 500;
}
.cp4-modal-btn.danger {
  background: #E07A5F;
  color: var(--white);
  font-weight: 500;
}
.cp4-modal-btn.disabled {
  opacity: 0.5;
}
.cp4-modal-btn:active {
  opacity: 0.8;
}
.cp4-modal-close {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  font-size: 40rpx;
  color: var(--ink-faint);
  font-weight: 300;
}
.cp4-reason-list {
  margin: 24rpx 0;
}
.cp4-reason-item {
  padding: 24rpx;
  margin-bottom: 12rpx;
  background: var(--brand-weak);
  border-radius: 16rpx;
  font-size: 28rpx;
  color: var(--ink);
  border: 2rpx solid transparent;
}
.cp4-reason-item.selected {
  border-color: var(--brand);
  background: var(--brand-tint);
  color: var(--brand-dark);
  font-weight: 500;
}
